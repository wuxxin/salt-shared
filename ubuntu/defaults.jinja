{% load_yaml as defaults %}
# switches applied to both states desktop and server
telemetry: false
snapd: false
backports: false

# switches only applied on state server
update:
  unattended_reboot: true
  allowed_origins: []
{% endload %}

{% set settings=salt['grains.filter_by']({'none': defaults},
  grain='none', default= 'none', merge= salt['pillar.get']('ubuntu', {})) %}
