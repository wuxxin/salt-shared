#!/usr/bin/bash

set -eo pipefail
#set -x

# clear all luks header slots with clevis config
# clear tang server entries before deleting clevis luks header
# should be run early, on systemstart

exit 1
