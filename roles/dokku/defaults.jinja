{% load_yaml as defaults %}

hostname: {{ salt['cmd.run_stdout']('hostname -f') }}

{% endload %}

{% set settings=salt['grains.filter_by']({'none': defaults},
  grain='none', default= 'none', merge= salt['pillar.get']('dokku', {})) %}
