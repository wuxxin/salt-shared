{% load_yaml as defaults %}

dev_version: false
kernel:
  version: 4.2
  patches:
    - https://launchpadlibrarian.net/149319334/overlayfs_inotify.patch

{% endload %}

{% set settings=salt['grains.filter_by']({'none': defaults},
  grain='none', default= 'none', merge= salt['pillar.get']('docker', {})) %}
