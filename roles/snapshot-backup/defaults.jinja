{% load_yaml as defaults %} 

backup_vm_name: 'backup_vm'
config_base: '/srv/snapshot-backup/'
snapshot_size: '2g'

{% endload %}

{% set settings=salt['grains.filter_by']({'none': defaults},
  grain='none', default= 'none', merge= salt['pillar.get']('snapshot-backup:host', {})) %}

