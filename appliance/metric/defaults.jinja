{% load_yaml as defaults %}
metric_exporter: "cadvisor node-exporter process-exporter postgres_exporter"
metric_server: "prometheus alertmanager"
metric_gui: "grafana"
  
{% endload %}

{% set settings=salt['grains.filter_by']({'none': defaults},
  grain='none', default= 'none', merge= salt['pillar.get']('appliance:metric', {})) %}
