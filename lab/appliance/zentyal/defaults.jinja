{% load_yaml as defaults %}

admin:
  user: admin
  password:
languages:
letsencrypt:
  enabled: false
  domains:
  hook: /usr/local/etc/dehydrated/zentyal-dehydrated-hook.sh
dkim:
  key: 
samba:
  bridge:
    ipaddr: 10.183.220.1
    netmask: 255.255.255.0
{% endload %}

{% set settings=salt['grains.filter_by']({'none': defaults},
  grain='none', default= 'none', merge= salt['pillar.get']('appliance:zentyal', {})) %}
