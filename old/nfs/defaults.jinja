{% load_yaml as defaults %}
listen_ip:
  - '127.0.0.1'
  - '::1'
{% endload %}

{# merge defaults with pillar #}
{% set settings=salt['grains.filter_by']({'none': defaults},
  grain='none', default= 'none', merge= salt['pillar.get']('nfs', {})) %}
